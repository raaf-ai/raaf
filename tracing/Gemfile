# frozen_string_literal: true

source "https://rubygems.org"

# Use local raaf-core gem
gem "raaf-core", path: "../core"

# Specify your gem's dependencies in the gemspec
gemspec

group :development do
  gem "rake", "~> 13.0"
  gem "yard", "~> 0.9"
end

group :test do
  gem "benchmark" # No longer a default gem as of Ruby 3.5
  gem "rspec", "~> 3.0"
  gem "rspec-collection_matchers", "~> 1.2" # specs use expect(x).to have(n).items
  gem "simplecov", "~> 0.22"
  gem "vcr", "~> 6.1"
  gem "webmock", "~> 3.18"
end

group :development, :test do
  gem "pry", "~> 0.14"
  gem "pry-byebug", "~> 3.10"
  gem "rubocop", "~> 1.21"
  gem "rubocop-rake", "~> 0.6"
  gem "rubocop-rspec", "~> 3.0"
end
