<%
  layout = RubyAIAgentsFactory::Tracing::BaseLayout.new(title: "Spans")
  spans_list = RubyAIAgentsFactory::Tracing::SpansList.new(spans: @spans)
%>

<%= render layout do %>
  <%= render spans_list %>
<% end %>