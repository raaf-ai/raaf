// Update events list via JavaScript
console.log('Loading events for span...');

var eventsContainer = document.querySelector('#events-container');
if (eventsContainer) {
  eventsContainer.innerHTML = '<%= j(render partial: "events_list", locals: { events: @events }) %>';
}