// Update tools list via JavaScript
console.log('Updating tool calls list...');

// This would typically update the page content dynamically
// For now, just reload the page to show the filtered results
window.location.reload();